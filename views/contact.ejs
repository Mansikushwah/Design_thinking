<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Contact Us - SkillConnect</title>
    <style>
        /* Base Styles */
        body {
            font-family: 'Arial', sans-serif;
            background-color: white; /* Lighter background */
            color: #333;
            margin: 0;
            padding: 0;
        }

        /* Navigation Bar */
        header {
            background-color: #27374D; /* Dark for the navbar */
            padding: 15px 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header h1 {
            font-size: 2.5em;
            margin: 0;
            color: white; /* Light text for dark navbar */
        }

        header nav {
            display: flex;
            align-items: center;
            margin-right: 3vw;        
        }

        header nav a {
            color: white; /* Light text for contrast */
            text-decoration: none;
            font-weight: bold;
            margin: 0 15px;
            transition: color 0.3s ease;
            padding: 5px 10px;
        }

        /* Style for active page */
        header nav a.active {
            background-color: #9DB2BF; /* Medium tone for active link */
            color: black; /* Dark text on active link */
            border-radius: 5px;
        }

        header nav a:hover {
            transform: scale(1.05);
        }

        /* Main Content */
        main {
            padding: 80px 20px 40px;
            text-align: center;
            margin-top: 60px; /* Ensure content does not hide behind header */
        }

        main h2 {
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #27374D; /* Dark tone for headings */
        }

        main p {
            font-size: 1.2em;
            color: #666;
        }

        /* Contact Form */
        .contact-form {
            background-color: #DDE6ED; /* Light background for form */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            display: inline-block;
            text-align: left;
            max-width: 600px;
            margin: 0 auto;
            width: 100%;
        }

        .contact-form label {
            display: block;
            font-size: 1.1em;
            margin: 10px 0 5px;
            color: #333;
        }

        .contact-form input, .contact-form textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #27374D; /* Darker border */
            border-radius: 5px;
            margin-bottom: 15px;
        }

        .contact-form button {
            background-color: #27374D; /* Dark button */
            color: white; /* Light text */
            border: none;
            padding: 10px 20px;
            font-size: 1.2em;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .contact-form button:hover {
            background-color: #9DB2BF; /* Lighter on hover */
        }

        /* Footer */
        footer {
            background-color: #DDE6ED;
            padding: 20px;
            text-align: center;
            color: #333;
            box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.1);
        }

        footer h3 {
            margin: 0 0 10px;
            font-size: 1.5em;
            color: #27374D;
        }

        footer p {
            font-size: 1em;
            margin: 0;
            color: #333;
        }

        footer a {
            color: #333;
            text-decoration: none;
            font-weight: bold;
            margin: 0 10px;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: #9DB2BF;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .contact-form {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>SkillConnect</h1>
        <nav>
            <% if (user) { %>
                <a href="/" class="<%= (page === 'home') ? 'active' : '' %>">Home</a>
                <a href="/contact" class="active">Contact Us</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/" class="<%= (page === 'home') ? 'active' : '' %>">Home</a>
                <a href="/contact" class="active">Contact Us</a>
                <a href="/login">Login</a>
                <a href="/register">Register</a>
                <a href="/about">About Us</a>
            <% } %>
        </nav>
    </header>

    <main>
        <section>
            <h2>Get in Touch</h2>
            <form class="contact-form" action="/contact" method="POST">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required>
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
                <label for="message">Message:</label>
                <textarea id="message" name="message" rows="5" required></textarea>
                <button type="submit">Send Message</button>
            </form>
        </section>
    </main>

    <footer>
        <h3>SkillConnect</h3>
        <p>&copy; 2024 SkillConnect. All rights reserved.</p>
        <a href="/about">About Us</a> | <a href="/privacy">Privacy Policy</a>
    </footer>
</body>
</html>
