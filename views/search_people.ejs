<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillConnect - People Search</title>
    <style>
        /* Base Styles */
        body {
            font-family: 'Arial', sans-serif;
            background-color: white;
            color: #333;
            margin: 0;
            padding: 0;
        }

        /* Header */
        header {
            background-color: #27374D; /* Dark for the navbar */
            padding: 15px 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header h1 {
            font-size: 2.5em;
            margin: 0;
            color: white;
        }

        header nav {
            display: flex;
            align-items: center;
            margin-right: 3vw;
        }

        header nav a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            margin: 0 15px;
            transition: color 0.3s ease;
        }

        header nav a:hover {
            color: #9DB2BF;
        }

        /* Main Content */
        main {
            padding: 80px 20px 40px;
            text-align: center;
            margin-top: 60px;
        }

        .hero {
            background: linear-gradient(#526D82, rgba(255, 255, 255, 0.8)), url('/images/hero-bg.jpg') no-repeat center center/cover;
            padding: 80px 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }

        .hero h2 {
            font-size: 2.5em;
            margin: 0 0 20px;
            color: black;
        }

        .hero p {
            font-size: 1.2em;
            margin-bottom: 30px;
            color: #666;
        }

        /* Search Section */
        .search-container {
            margin-bottom: 30px;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            /* box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); */
        }

        .search-container input[type="text"] {
            width: 60%;
            padding: 12px;
            font-size: 16px;
            border: 1px solid #CCC;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        .search-container button {
            padding: 12px 25px;
            font-size: 16px;
            margin-left: 10px;
            border: none;
            background-color: #27374D;
            color: white;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .search-container button:hover {
            background-color: #526D82;
        }

        /* People Cards Section */
        #peopleContainer {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .person-card {
            background-color: #DDE6ED;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin: 20px;
            text-align: center;
            width: 28%;
        }

        .person-card img {
            border-radius: 50%;
            width: 100px;
            height: 100px;
            object-fit: cover;
            margin-bottom: 10px;
        }

        .person-card h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            color: #34322c;
        }

        .person-card p {
            font-size: 1em;
            color: #666;
        }

        /* Footer */
        footer {
            background-color: #DDE6ED;
            padding: 20px;
            text-align: center;
            color: #333;
            box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.1);
        }

        footer h3 {
            margin: 0 0 10px;
            font-size: 1.5em;
            color: #27374D;
        }

        footer p {
            font-size: 1em;
            margin: 0;
            color: #333;
        }

        footer a {
            color: #333;
            text-decoration: none;
            font-weight: bold;
            margin: 0 10px;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: #9DB2BF;
        }

        @media (max-width: 768px) {
            .person-card {
                width: 100%;
                margin-bottom: 20px;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>SkillConnect</h1>
        <nav>
            <% if (user) { %>
                <p>Hello, <%= user.username %>!</p>
                <a href="/search">Showcases</a>
                <a href="/post">Posts</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/">Home</a> | <a href="/login">Login</a> | <a href="/register">Register</a> | <a href="/about">About Us</a>
            <% } %>
        </nav>
    </header>

    <main>
        <!-- Hero Section -->
        <section class="hero">
            <h2>Search for Talented Professionals</h2>
            <p>At SkillConnect, we connect you with skilled professionals across different fields.</p>
        </section>

        <!-- Search Bar -->
        <div class="search-container">
            <input type="text" id="peopleSkillSearch" placeholder="Search by skill...">
            <button id="searchButton">Search</button>
        </div>

        <!-- People Cards -->
        <div id="peopleContainer">
            <% if (people.length > 0) { %>
                <% people.forEach(person => { %>
                    <div class="person-card">
                        <img src="<%= person.profile_photo_url || '/images/default_profile_photo.jpg' %>" alt="<%= person.username %>">
                        <h3><%= person.username %></h3>
                        <p>Skills: <%= person.skills %></p>
                        <p>Email: <%= person.email %></p>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No people found with that skill.</p>
            <% } %>
        </div>
    </main>

    <!-- Footer Section -->
    <footer>
        <h3>SkillConnect</h3>
        <p>&copy; 2024 SkillConnect. All rights reserved.</p>
        <a href="/contact">Contact Us</a> | <a href="/privacy">Privacy Policy</a>
    </footer>

    <script>
        document.getElementById('searchButton').addEventListener('click', async function () {
            const skill = document.getElementById('peopleSkillSearch').value.toLowerCase();
            const response = await fetch(`/people/search?skill=${skill}`);
            const html = await response.text();
            document.getElementById('peopleContainer').innerHTML = html;
        });
    </script>
</body>
</html>
